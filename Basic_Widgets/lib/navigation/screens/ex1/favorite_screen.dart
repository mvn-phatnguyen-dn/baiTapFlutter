import 'package:flutter/material.dart';

class FavoritesScreenA extends StatelessWidget {
  const FavoritesScreenA({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favorites Screen'),
    );
  }
}
