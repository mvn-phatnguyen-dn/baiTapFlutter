import 'package:flutter/material.dart';

class MapScreenA extends StatelessWidget {
  const MapScreenA({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Map Screen'),
    );
  }
}